# Schematic Converter

Convert KiCad schematic files to JSON.

## Usage

```bash
npm start path/to/file.kicad_sch
```

or

```bash
npm start path/to/file.sch
```

Outputs `context.json` in the current directory.
# schematicConverter
